import AsyncStorage from "@react-native-async-storage/async-storage";
import { Ionicons } from "@expo/vector-icons";
import * as Haptics from "expo-haptics";
import { useMemo, useState } from "react";
import { Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { destinations, searchDestinations, type Destination } from "@/lib/india";
import { calculatePlanBudget, generateItinerary, type DayPlan } from "@/lib/trip-planner";

const C = { navy: "#0D2340", coral: "#F06C4E", muted: "#718096", line: "#E5EAF0", soft: "#FFF0EA", pale: "#F5F8FB", white: "#FFFFFF", green: "#1F9D74", danger: "#D54A48" };
const stylesList = ["Budget", "Standard", "Premium"] as const;
const interests = ["Famous places", "Nature", "Adventure", "History", "Food", "Shopping", "Culture", "Relaxation"];
type Style = typeof stylesList[number];
type SavedTrip = { destination: string; startDate: string; days: number; travellers: number; budget: number; style: Style; interests: string[]; itinerary: DayPlan[]; savedAt: string };

function haptic() { if (Platform.OS !== "web") void Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light); }
function parseDate(value: string) { const match = value.trim().match(/^(\d{1,2})[\s/-]+([A-Za-z]+|\d{1,2})[\s/-]+(\d{4})$/); if (!match) return null; const monthNames = ["january", "february", "march", "april", "may", "june", "july", "august", "september", "october", "november", "december"]; const month = Number.isNaN(Number(match[2])) ? monthNames.indexOf(match[2].toLowerCase()) : Number(match[2]) - 1; const date = new Date(Number(match[3]), month, Number(match[1])); return month >= 0 && month < 12 && date.getFullYear() === Number(match[3]) && date.getMonth() === month && date.getDate() === Number(match[1]) ? date : null; }
function dateLabel(date: Date) { return date.toLocaleDateString("en-IN", { day: "numeric", month: "long", year: "numeric" }); }
function addDays(start: Date, amount: number) { const next = new Date(start); next.setDate(next.getDate() + amount); return next; }

export default function AITripPlanner({ onClose }: { onClose: () => void }) {
  const [destination, setDestination] = useState<Destination | null>(null);
  const [search, setSearch] = useState("");
  const [searchOpen, setSearchOpen] = useState(false);
  const [dateInput, setDateInput] = useState("");
  const [daysInput, setDaysInput] = useState("3");
  const [travellersInput, setTravellersInput] = useState("2");
  const [budgetInput, setBudgetInput] = useState("10000");
  const [travelStyle, setTravelStyle] = useState<Style>("Standard");
  const [selectedInterests, setSelectedInterests] = useState<string[]>(["Famous places", "Culture"]);
  const [itinerary, setItinerary] = useState<DayPlan[] | null>(null);
  const [notice, setNotice] = useState("");
  const [chatInput, setChatInput] = useState("");
  const [chatReply, setChatReply] = useState("");
  const [saved, setSaved] = useState(false);
  const [savedTrips, setSavedTrips] = useState<SavedTrip[]>([]);
  const filtered = useMemo(() => searchDestinations(search).slice(0, 8), [search]);
  const days = Math.max(1, Math.min(30, Number(daysInput) || 0));
  const travellers = Math.max(1, Math.min(12, Number(travellersInput) || 0));
  const budget = Number(budgetInput.replace(/[^0-9]/g, "")) || 0;
  const startDate = parseDate(dateInput);
  const planBudget = destination ? calculatePlanBudget({ destination, budget, days, travellers }) : null;

  const chooseDestination = (item: Destination) => { setDestination(item); setSearchOpen(false); setSearch(""); setItinerary(null); setNotice(""); haptic(); };
  const generate = () => { if (!destination || !startDate || days < 1 || travellers < 1 || budget < 1) { setNotice("Enter a supported destination, a valid date such as 10 October 2026, number of days, travellers, and budget."); return; } const next = generateItinerary({ destination, budget, days, travellers }); if (!next.length) { setNotice(`Verified attraction data is not available for ${destination.name}. The assistant will not invent places, prices, hours, or distances.`); return; } setNotice(""); setSaved(false); setItinerary(next); haptic(); };
  const regenerate = () => { if (destination) { setItinerary(generateItinerary({ destination, budget, days, travellers })); setChatReply("I regenerated the current trip while keeping your exact destination, dates, traveller count, and budget."); } };
  const saveTrip = async () => { if (!destination || !startDate || !itinerary) return; const trip: SavedTrip = { destination: destination.name, startDate: dateInput, days, travellers, budget, style: travelStyle, interests: selectedInterests, itinerary, savedAt: new Date().toISOString() }; const existing = await AsyncStorage.getItem("voyage.savedTrips"); const next = [trip, ...(existing ? JSON.parse(existing) as SavedTrip[] : [])]; await AsyncStorage.setItem("voyage.savedTrips", JSON.stringify(next)); setSavedTrips(next); setSaved(true); setChatReply("Trip saved to your local Trips section."); haptic(); };
  const sendChat = () => { const command = chatInput.trim().toLowerCase(); if (!command || !destination) return; if (command.includes("extra day")) { setDaysInput(String(days + 1)); setChatReply("I added one day and kept the existing trip details. Press Regenerate to rebuild the exact-date itinerary."); } else if (command.includes("cheaper") || command.includes("only have")) { setTravelStyle("Budget"); setChatReply("I switched the planning style to Budget. The current estimates remain labelled estimated; press Regenerate to apply the cheaper approach."); } else if (command.includes("adventure")) { setSelectedInterests((items) => items.includes("Adventure") ? items : [...items, "Adventure"]); setChatReply("Adventure is now selected. Press Regenerate to apply it to the current trip."); } else if (command.includes("shopping")) { setSelectedInterests((items) => items.filter((item) => item !== "Shopping")); setChatReply("Shopping was removed from the interests. Press Regenerate to apply it."); } else if (command.includes("famous")) { setSelectedInterests((items) => items.includes("Famous places") ? items : [...items, "Famous places"]); setChatReply("I added Famous places to this trip. Press Regenerate to apply it."); } else if (command.includes("family")) { setTravelStyle("Standard"); setChatReply("I will keep the current trip practical for a family by using a Standard style. Press Regenerate to apply it."); } else if (command.includes("hotel")) setChatReply("Accommodation is currently an estimate. Live hotel availability is not connected yet, so I will not invent a hotel or price."); else if (command.includes("travel time")) setChatReply("I can reduce travel time only when live distance and traffic data is connected. The current route uses curated nearby groupings and is labelled estimated."); else setChatReply("I can update this same trip for budget, famous places, adventure, shopping, an extra day, family planning, hotel constraints, or travel time."); setChatInput(""); };

  return <ScreenContainer containerClassName="bg-[#F7F9FC]" className="px-5"><ScrollView contentContainerStyle={s.scroll} keyboardShouldPersistTaps="handled">
    <View style={s.topbar}><Pressable onPress={onClose} style={s.back}><Ionicons name="arrow-back" size={18} color={C.navy} /></Pressable><View style={{ flex: 1, marginLeft: 11 }}><Text style={s.eyebrow}>VOYAGE AI ASSISTANT</Text><Text style={s.heading}>Plan My Trip</Text></View><Ionicons name="sparkles" size={20} color={C.coral} /></View>
    {!itinerary ? <><View style={s.hero}><Text style={s.heroKicker}>EXACT DATES · REAL CONSTRAINTS</Text><Text style={s.heroTitle}>A personal plan for{`\n`}your India trip.</Text><Text style={s.heroMeta}>Tell the assistant where, when, how long, who is travelling, your budget, style and interests.</Text></View><View style={s.card}><Text style={s.label}>1 · WHERE DO YOU WANT TO GO?</Text><Pressable style={s.select} onPress={() => setSearchOpen(true)}><Ionicons name="location-outline" size={18} color={C.coral} /><View style={{ flex: 1, marginLeft: 8 }}>{destination ? <><Text style={s.value}>{destination.name}</Text><Text style={s.hint}>{destination.state} · {destination.type}</Text></> : <Text style={s.placeholder}>Search any supported Indian city or district</Text>}</View><Ionicons name="chevron-down" size={16} color={C.muted} /></Pressable>{searchOpen && <View style={s.searchPanel}><View style={s.searchBox}><Ionicons name="search" size={16} color={C.muted} /><TextInput autoFocus value={search} onChangeText={setSearch} placeholder="City, district, state, or code" placeholderTextColor="#A0AAB8" style={s.searchInput} /></View>{filtered.map((item) => <Pressable key={item.code} style={s.result} onPress={() => chooseDestination(item)}><Ionicons name="location-outline" size={16} color={C.coral} /><View style={{ flex: 1, marginLeft: 8 }}><Text style={s.value}>{item.name}</Text><Text style={s.hint}>{item.state} · {item.type}</Text></View><Text style={s.code}>{item.code}</Text></Pressable>)}</View>}
      <Text style={s.label}>2 · TRIP START DATE</Text><TextInput value={dateInput} onChangeText={setDateInput} placeholder="10 October 2026" placeholderTextColor="#A0AAB8" style={s.input} /><Text style={s.helper}>Use the exact date. The generated days will show calendar dates from this start date.</Text>
      <Text style={s.label}>3 · HOW MANY DAYS?</Text><TextInput value={daysInput} onChangeText={setDaysInput} keyboardType="number-pad" placeholder="3" placeholderTextColor="#A0AAB8" style={s.input} />
      <Text style={s.label}>4 · TRAVELLERS</Text><TextInput value={travellersInput} onChangeText={setTravellersInput} keyboardType="number-pad" placeholder="2" placeholderTextColor="#A0AAB8" style={s.input} />
      <Text style={s.label}>5 · TOTAL BUDGET</Text><View style={s.money}><Text style={s.rupee}>₹</Text><TextInput value={budgetInput} onChangeText={setBudgetInput} keyboardType="number-pad" style={s.moneyText} /></View>
      <Text style={s.label}>6 · PREFERRED TRAVEL STYLE</Text><View style={s.chips}>{stylesList.map((item) => <Pressable key={item} onPress={() => setTravelStyle(item)} style={[s.chip, travelStyle === item && s.chipActive]}><Text style={[s.chipText, travelStyle === item && { color: C.white }]}>{item}</Text></Pressable>)}</View>
      <Text style={s.label}>7 · INTERESTS</Text><View style={s.chips}>{interests.map((item) => { const active = selectedInterests.includes(item); return <Pressable key={item} onPress={() => setSelectedInterests((current) => active ? current.filter((value) => value !== item) : [...current, item])} style={[s.chip, active && s.chipActive]}><Text style={[s.chipText, active && { color: C.white }]}>{item}</Text></Pressable>; })}</View>
    </View>{notice && <Text style={s.error}>{notice}</Text>}<Pressable onPress={generate} style={s.primary}><Text style={s.primaryText}>Generate My AI Itinerary</Text><Ionicons name="sparkles-outline" size={18} color={C.white} /></Pressable><Text style={s.disclaimer}>Places, hours, fees, distances, transport, hotels and prices are never presented as confirmed unless live data is available.</Text></> : <><View style={s.planHeader}><View style={{ flex: 1 }}><Text style={s.eyebrow}>YOUR PERSONALIZED AI PLAN</Text><Text style={s.planTitle}>{destination?.name}</Text><Text style={s.planMeta}>{dateInput} · {days} days · {travellers} travellers · {travelStyle}</Text><Text style={s.planMeta}>{selectedInterests.join(" · ") || "No specific interests"}</Text></View><Pressable onPress={() => setItinerary(null)}><Text style={s.link}>Edit</Text></Pressable></View>{planBudget && <View style={s.breakdown}><Text style={s.sectionTitle}>Complete budget estimate</Text><BudgetLine label="Travel to destination + return" value={planBudget.travel} /><BudgetLine label="Accommodation" value={planBudget.accommodation} /><BudgetLine label="Food" value={planBudget.food} /><BudgetLine label="Local transportation" value={planBudget.local} /><BudgetLine label="Entry tickets + activities" value={planBudget.activities} /><BudgetLine label="Emergency / miscellaneous buffer" value={planBudget.emergency} /><View style={s.total}><Text style={s.totalLabel}>Total Budget</Text><Text style={s.totalValue}>₹{budget.toLocaleString("en-IN")}</Text></View><View style={s.total}><Text style={s.totalLabel}>Estimated Cost</Text><Text style={s.totalValue}>₹{planBudget.total.toLocaleString("en-IN")}{planBudget.isTight ? " · tight" : ""}</Text></View><View style={s.total}><Text style={s.totalLabel}>Remaining Budget</Text><Text style={[s.totalValue, { color: planBudget.isTight ? C.danger : C.green }]}>₹{planBudget.remaining.toLocaleString("en-IN")}</Text></View></View>}<Text style={s.sectionTitle}>Day-by-day itinerary</Text>{itinerary.map((day) => <DayCard key={day.day} day={day} startDate={startDate} />)}<View style={s.chatCard}><Text style={s.sectionTitle}>Ask about this trip</Text><Text style={s.hint}>Change the current itinerary, not a separate trip.</Text><View style={s.chatRow}><TextInput value={chatInput} onChangeText={setChatInput} placeholder="Make this trip cheaper..." placeholderTextColor="#A0AAB8" style={s.chatInput} /><Pressable onPress={sendChat} style={s.send}><Ionicons name="arrow-up" size={17} color={C.white} /></Pressable></View>{chatReply && <Text style={s.chatReply}>{chatReply}</Text>}</View><View style={s.actions}><Pressable onPress={saveTrip} style={s.primarySmall}><Ionicons name={saved ? "checkmark-circle" : "bookmark"} size={16} color={C.white} /><Text style={s.primaryText}>{saved ? "Saved to Trips" : "Save Trip"}</Text></Pressable><Pressable onPress={regenerate} style={s.secondary}><Ionicons name="refresh" size={16} color={C.navy} /><Text style={s.secondaryText}>Regenerate</Text></Pressable></View><Pressable onPress={() => setItinerary(null)} style={s.editButton}><Text style={s.link}>Edit Trip Details</Text></Pressable><Text style={s.disclaimer}>Estimated information must be verified before booking. Live hotel, transport, price and availability APIs are not connected in this demo.</Text></>}
  </ScrollView></ScreenContainer>;
}

function BudgetLine({ label, value }: { label: string; value: number }) { return <View style={s.budgetLine}><Text style={s.budgetLabel}>{label}</Text><Text style={s.budgetValue}>₹{value.toLocaleString("en-IN")}</Text></View>; }
function DayCard({ day, startDate }: { day: DayPlan; startDate: Date | null }) { const exactDate = startDate ? dateLabel(addDays(startDate, day.day - 1)) : "Date unavailable"; return <View style={s.dayCard}><View style={s.dayHeader}><View style={s.dayBubble}><Text style={s.dayNo}>{day.day}</Text></View><View style={{ flex: 1 }}><Text style={s.dayTitle}>Day {day.day} — {exactDate}</Text><Text style={s.daySub}>{day.title}</Text></View><Text style={s.dayCost}>₹{day.cost.toLocaleString("en-IN")}</Text></View><Text style={s.order}>{day.stops.length ? day.order : "No additional verified place record for this day"}</Text>{day.stops.map((stop, index) => <View key={stop.name} style={s.stop}><Text style={s.stopTitle}>{index + 1}. {stop.name}</Text><Text style={s.stopAddress}>{stop.address}</Text><Text style={s.stopText}>{stop.why}</Text><Text style={s.stopText}>Visit: {stop.duration} · Best time: {stop.bestTime}</Text><Text style={s.stopText}>Hours: {stop.hours}</Text><Text style={s.stopText}>Entry: {stop.entryFee}</Text><Text style={s.stopText}>From previous: {stop.fromPrevious} · {stop.transport}</Text><Text style={s.stopText}>Food: {stop.nearbyFood}</Text><Text style={s.stopText}>Tips: {stop.tips}</Text></View>)}<Text style={s.dayNote}>{day.transport} · {day.food} · {day.stay}</Text><Text style={s.dayNote}>{day.note}</Text></View>; }

const s = StyleSheet.create({ scroll: { paddingTop: 14, paddingBottom: 40 }, topbar: { flexDirection: "row", alignItems: "center", marginBottom: 18 }, back: { width: 38, height: 38, borderRadius: 12, backgroundColor: C.white, alignItems: "center", justifyContent: "center" }, eyebrow: { color: C.coral, fontSize: 9, fontWeight: "900", letterSpacing: 1.1 }, heading: { color: C.navy, fontSize: 23, fontWeight: "900", marginTop: 3 }, hero: { backgroundColor: C.navy, borderRadius: 20, padding: 18, marginBottom: 18 }, heroKicker: { color: "#A9BCD0", fontSize: 9, fontWeight: "900", letterSpacing: 1 }, heroTitle: { color: C.white, fontSize: 24, lineHeight: 29, fontWeight: "900", marginTop: 8 }, heroMeta: { color: "#C1CFDC", fontSize: 11, lineHeight: 16, marginTop: 8 }, card: { backgroundColor: C.white, borderRadius: 17, padding: 15, borderWidth: 1, borderColor: C.line }, label: { color: C.muted, fontSize: 9, fontWeight: "900", letterSpacing: 0.7, marginTop: 15, marginBottom: 7 }, select: { minHeight: 50, borderRadius: 11, backgroundColor: C.pale, flexDirection: "row", alignItems: "center", paddingHorizontal: 11 }, value: { color: C.navy, fontSize: 12, fontWeight: "900" }, hint: { color: C.muted, fontSize: 9, marginTop: 2 }, placeholder: { color: "#9AA7B5", fontSize: 11 }, searchPanel: { marginTop: 10, borderTopWidth: 1, borderColor: C.line, paddingTop: 10 }, searchBox: { height: 42, backgroundColor: C.pale, borderRadius: 10, flexDirection: "row", alignItems: "center", paddingHorizontal: 10 }, searchInput: { flex: 1, marginLeft: 7, color: C.navy, fontSize: 11 }, result: { flexDirection: "row", alignItems: "center", paddingVertical: 8 }, code: { color: C.muted, fontSize: 9, fontWeight: "900" }, input: { height: 47, borderRadius: 11, backgroundColor: C.pale, paddingHorizontal: 12, color: C.navy, fontSize: 13 }, helper: { color: C.muted, fontSize: 9, lineHeight: 14, marginTop: 5 }, money: { height: 47, borderRadius: 11, backgroundColor: C.pale, flexDirection: "row", alignItems: "center", paddingHorizontal: 12 }, rupee: { color: C.coral, fontSize: 19, fontWeight: "900", marginRight: 6 }, moneyText: { flex: 1, color: C.navy, fontSize: 16, fontWeight: "900" }, chips: { flexDirection: "row", flexWrap: "wrap", gap: 7 }, chip: { borderWidth: 1, borderColor: C.line, borderRadius: 20, paddingVertical: 8, paddingHorizontal: 11, backgroundColor: C.white }, chipActive: { backgroundColor: C.coral, borderColor: C.coral }, chipText: { color: C.navy, fontSize: 10, fontWeight: "800" }, error: { color: C.danger, backgroundColor: "#FFF0EF", borderRadius: 10, padding: 10, fontSize: 10, lineHeight: 15, marginTop: 10 }, primary: { height: 52, borderRadius: 14, backgroundColor: C.coral, flexDirection: "row", gap: 8, alignItems: "center", justifyContent: "center", marginTop: 14 }, primarySmall: { flex: 1, height: 46, borderRadius: 12, backgroundColor: C.coral, flexDirection: "row", gap: 7, alignItems: "center", justifyContent: "center" }, primaryText: { color: C.white, fontSize: 12, fontWeight: "900" }, disclaimer: { color: "#9AA7B5", textAlign: "center", fontSize: 9, lineHeight: 14, marginTop: 10 }, planHeader: { flexDirection: "row", alignItems: "flex-start", marginBottom: 14 }, planTitle: { color: C.navy, fontSize: 24, fontWeight: "900", marginTop: 4 }, planMeta: { color: C.muted, fontSize: 10, marginTop: 4 }, link: { color: C.coral, fontWeight: "900", fontSize: 11 }, breakdown: { backgroundColor: C.white, borderRadius: 16, padding: 14, borderWidth: 1, borderColor: C.line, marginBottom: 16 }, sectionTitle: { color: C.navy, fontSize: 16, fontWeight: "900", marginBottom: 8 }, budgetLine: { flexDirection: "row", justifyContent: "space-between", borderBottomWidth: 1, borderColor: "#F0F3F6", paddingVertical: 8 }, budgetLabel: { color: C.muted, fontSize: 10 }, budgetValue: { color: C.navy, fontSize: 10, fontWeight: "800" }, total: { flexDirection: "row", justifyContent: "space-between", paddingTop: 9 }, totalLabel: { color: C.navy, fontSize: 11, fontWeight: "900" }, totalValue: { color: C.coral, fontSize: 12, fontWeight: "900" }, dayCard: { backgroundColor: C.white, borderRadius: 16, padding: 13, borderWidth: 1, borderColor: C.line, marginBottom: 10 }, dayHeader: { flexDirection: "row", alignItems: "center", gap: 8 }, dayBubble: { width: 33, height: 33, borderRadius: 11, backgroundColor: C.navy, alignItems: "center", justifyContent: "center" }, dayNo: { color: C.white, fontWeight: "900" }, dayTitle: { color: C.navy, fontSize: 11, fontWeight: "900" }, daySub: { color: C.muted, fontSize: 9, marginTop: 2 }, dayCost: { color: C.coral, fontSize: 11, fontWeight: "900" }, order: { color: C.coral, backgroundColor: C.soft, padding: 8, borderRadius: 9, fontSize: 9, lineHeight: 14, marginTop: 10 }, stop: { borderTopWidth: 1, borderColor: C.line, marginTop: 10, paddingTop: 9 }, stopTitle: { color: C.navy, fontSize: 12, fontWeight: "900" }, stopAddress: { color: C.muted, fontSize: 9, marginTop: 2 }, stopText: { color: C.muted, fontSize: 9, lineHeight: 14, marginTop: 5 }, dayNote: { color: "#8C98A6", fontSize: 9, lineHeight: 14, marginTop: 9 }, chatCard: { backgroundColor: C.white, borderRadius: 16, padding: 14, borderWidth: 1, borderColor: C.line, marginTop: 5 }, chatRow: { flexDirection: "row", alignItems: "center", marginTop: 10, gap: 7 }, chatInput: { flex: 1, height: 43, backgroundColor: C.pale, borderRadius: 10, paddingHorizontal: 10, color: C.navy, fontSize: 11 }, send: { width: 40, height: 40, borderRadius: 12, backgroundColor: C.coral, alignItems: "center", justifyContent: "center" }, chatReply: { color: C.navy, fontSize: 10, lineHeight: 15, backgroundColor: C.soft, borderRadius: 9, padding: 9, marginTop: 9 }, actions: { flexDirection: "row", gap: 8, marginTop: 10 }, secondary: { flex: 1, height: 46, borderRadius: 12, borderWidth: 1, borderColor: C.line, backgroundColor: C.white, flexDirection: "row", gap: 6, alignItems: "center", justifyContent: "center" }, secondaryText: { color: C.navy, fontSize: 11, fontWeight: "900" }, editButton: { alignItems: "center", padding: 12 } });
